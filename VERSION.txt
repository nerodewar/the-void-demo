The Void Demo v1.0.4 Offline iPad PWA

Launch-stability repair:
- Full 142 MB offline archive now caches after launch instead of competing with Play.
- Cache work pauses during the opening transition.
- Existing cached media is reused after updates instead of downloaded again.
- Old complete caches remain available until the replacement archive is ready.
- Image preloads have a timeout and no longer await expensive decode operations.
- Repeated Play taps are guarded against overlapping launches.
- Installed PWAs automatically reload once when a new service worker takes control.
- Ground Control terminal presentation remains enabled for all communication scenes.
